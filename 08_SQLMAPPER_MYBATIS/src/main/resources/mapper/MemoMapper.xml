<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.Domain.Common.Mapper.MemoMapper">

    <insert id="insertXML">
        insert into tbl_memo values(#{id},#{title},#{writer},#{text},#{createAt})
    </insert>

    <update id="updateXML">
        update tbl_memo set text=#{text} where id=#{id}
    </update>

    <delete id="deleteXML">
        delete from tbl_memo where id=#{id}
    </delete>

    <select id="selectOneXML" resultType="com.example.demo.Domain.Common.Dtos.MemoDTO" parameterType="Long">
        select * from tbl_memo where id=#{id}
    </select>

    <select id="selectAllXML" resultType="com.example.demo.Domain.Common.Dtos.MemoDTO">
        select * from tbl_memo
    </select>

    <select id="selectAllMapXML" resultType="java.util.Map">
        select * from tbl_memo
    </select>

    




</mapper>